# Demo App Environment Variables
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env

# =============================================================================
# DATABASE (Required)
# =============================================================================
# PostgreSQL connection string - used by Drizzle ORM
DATABASE_URL=postgresql://app:secret@localhost:5432/app

# Alternative: Individual connection variables (CloudNativePG style)
# These are ignored if DATABASE_URL is set
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=app
# PGUSER=app
# PGPASSWORD=secret

# =============================================================================
# SERVER
# =============================================================================
# API server port
PORT=3000

# CORS origin (set to frontend URL for local dev, false to disable)
CORS_ORIGIN=http://localhost:5173

# =============================================================================
# LOGGING
# =============================================================================
# Log level: debug, info, warn, error
LOG_LEVEL=debug

# Environment: development, production, test
NODE_ENV=development

# =============================================================================
# PR ENVIRONMENT METADATA (Optional)
# =============================================================================
# These are injected automatically in Kubernetes preview environments
# Set them manually for local testing if needed

# PR_NUMBER=42
# COMMIT_SHA=abc123
# BRANCH_NAME=feat/my-feature
# APP_VERSION=1.0.0
# PREVIEW_URL=http://localhost:3000

# =============================================================================
# OPTIONAL DATABASES (docker-compose profiles)
# =============================================================================
# Enable these when using additional services with docker-compose profiles
# Example: docker compose --profile mongodb --profile redis up -d

# MongoDB (--profile mongodb)
# MONGODB_URL=mongodb://app:secret@localhost:27017/app?authSource=admin

# Redis (--profile redis)
# REDIS_URL=redis://localhost:6379

# MinIO (--profile minio)
# MINIO_ENDPOINT=localhost
# MINIO_PORT=9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin

# MariaDB (--profile mariadb)
# MARIADB_URL=mysql://app:secret@localhost:3306/app
